import { PhotosPage } from '@/components/photos/PhotosPage'

/** /photos — the merchant library (docs/CONTRACTS.md §23). /content stays as the advanced library. */
export default function Page() {
  return <PhotosPage />
}
